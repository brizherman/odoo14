<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_pos_cash_reallocation_wizard_form" model="ir.ui.view">
        <field name="name">pos.cash.reallocation.wizard.form</field>
        <field name="model">pos.cash.reallocation.wizard</field>
        <field name="arch" type="xml">
            <form string="Cash Reallocation">
                <header>
                    <button name="action_preview"
                            string="Preview"
                            type="object"
                            class="oe_highlight"
                            states="draft"/>
                    <button name="action_confirm"
                            string="Confirm"
                            type="object"
                            class="oe_highlight"
                            attrs="{'invisible': [('state', '!=', 'preview')]}"
                            confirm="Apply cash reallocation to the previewed orders? Closed-session runs will also post compensating journal entries."/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,preview,done"/>
                </header>
                <sheet>
                    <field name="include_closed_sessions" invisible="1"/>
                    <field name="has_run_search" invisible="1"/>
                    <field name="has_locked_fiscal_period" invisible="1"/>
                    <field name="available_session_ids" invisible="1"/>
                    <div class="alert alert-warning" role="alert"
                         attrs="{'invisible': [('include_closed_sessions', '=', False)]}">
                        Posts journal entries. Cannot modify bank statements.
                    </div>
                    <div class="alert alert-warning" role="alert"
                         attrs="{'invisible': [('has_locked_fiscal_period', '=', False)]}">
                        <field name="locked_session_warning" nolabel="1" readonly="1"/>
                    </div>
                    <group>
                        <group>
                            <field name="company_id"
                                   groups="base.group_multi_company"
                                   attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                            <field name="date_from"
                                   attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                            <field name="date_to"
                                   attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                        </group>
                        <group>
                            <field name="is_cash_count"/>
                            <field name="include_closed_sessions"
                                   groups="pos_cash_reallocation_wizard.group_pos_cash_reallocation_manager"
                                   attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                            <field name="matched_order_count"/>
                            <field name="total_net_cash"/>
                            <field name="amount_to_reallocate"
                                   attrs="{'readonly': [('state', '!=', 'draft')]}"/>
                        </group>
                    </group>
                    <group attrs="{'invisible': [('has_run_search', '=', False)]}">
                        <field name="available_session_ids"
                               widget="many2many_tags"
                               readonly="1"
                               string="Sessions in Date Range"/>
                        <field name="session_ids"
                               widget="many2many_tags"
                               domain="[('id', 'in', available_session_ids)]"
                               attrs="{'readonly': [('state', '!=', 'draft')]}"
                               placeholder="All sessions (leave empty)"/>
                    </group>
                    <notebook>
                        <page string="Preview" name="preview"
                              attrs="{'invisible': [('state', '=', 'draft')]}">
                            <field name="preview_line_ids" nolabel="1">
                                <tree string="Preview" create="0" delete="0" edit="0">
                                    <field name="order_id"/>
                                    <field name="session_id"
                                           attrs="{'column_invisible': [('parent.include_closed_sessions', '=', False)]}"/>
                                    <field name="original_cash"/>
                                    <field name="new_cash"/>
                                    <field name="wallet_amount" string="Lealtad Amount"/>
                                </tree>
                            </field>
                        </page>
                        <page string="Skipped Orders" name="skipped"
                              attrs="{'invisible': [('has_run_search', '=', False)]}">
                            <field name="skipped_line_ids" nolabel="1">
                                <tree string="Skipped Orders" create="0" delete="0" edit="0">
                                    <field name="order_id"/>
                                    <field name="skip_reason"/>
                                </tree>
                            </field>
                        </page>
                        <page string="History" name="history">
                            <field name="history_log_ids" nolabel="1">
                                <tree string="Cash Reallocation History" create="0" delete="0" edit="0">
                                    <field name="name" decoration-bf="1"/>
                                    <field name="create_date"/>
                                    <field name="user_id"/>
                                    <field name="company_id" groups="base.group_multi_company"/>
                                    <field name="reallocation_mode" optional="show"/>
                                    <field name="date_from"/>
                                    <field name="date_to"/>
                                    <field name="total_amount"/>
                                    <field name="order_count"/>
                                    <field name="state" widget="badge"
                                           decoration-success="state == 'done'"
                                           decoration-muted="state == 'reverted'"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_pos_cash_reallocation_wizard" model="ir.actions.act_window">
        <field name="name">Cash Reallocation</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">pos.cash.reallocation.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">current</field>
        <field name="view_id" ref="view_pos_cash_reallocation_wizard_form"/>
    </record>

</odoo>
